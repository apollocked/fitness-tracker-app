import 'package:flutter/material.dart';

const Color primaryColor = Color(0xFFD4AF37); // Gold
const Color secondaryColor = Color(0xFFB38B22); // Darker Gold
const Color blueColor = Color(0xFF2962FF); // Keeping constant for other UI elements if strictly needed, but not used as secondary
const Color orangeColor = Colors.orange;
const Color redColor = Colors.red;
const Color greenColor = Colors.green;
const Color blackColor = Color(0xFF14213D);
const Color backgroundColor = Colors.white;
const Color greyColor = Colors.grey;
const Color grey50Color = Color(0xFFFAFAFA);
const Color secondColor = Color(0xFF757575);

// Gradients
const List<Color> primaryGradient = [Color(0xFFEAD284), Color(0xFFD4AF37)]; // Light gold to Gold
const List<Color> greenGradient = [Color(0xFF22C55E), Color(0xFF16A34A)];
const List<Color> orangeGradient = [Color(0xFFF97316), Color(0xFFEF4444)];
const List<Color> purpleGradient = [Color(0xFF8B5CF6), Color(0xFFEC4899)];
const List<Color> lightBlueGradient = [Color(0xFF0EA5E9), Color(0xFF6366F1)];

// Dark mode colors
const Color darkBg = Color(0xFF121212);
const Color darkCard = Color(0xFF1E1E1E);
const Color darkCardSecondary = Color(0xFF2A2A2A);
const Color darkText = Color(0xFFE0E0E0);
